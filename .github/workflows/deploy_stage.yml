name: Deploy to Statge

on:
  push:
    branches:
      - master
      - main

concurrency:
  group: deploy-stage-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  versioning:
    runs-on: ubuntu-18.04

    steps:
      - name: Send deploy message
        uses: luisghz/simple-ms-teams-webhook-notifier@v1-latest
        with:
          webhook_url: ${{ secrets.MS_TEAMS_WEBHOOK_URI }}
          theme-color: Success
          summary: Sucessfull deploy of unknown version
          title: Sucessfull deploy of unknown version
          sections: |
            - activityImage: "{gh:avatar-url}"
              activityTitle: "PR is pending merge for MoveUI release using [this commit](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})"
              activitySubtitle: "Pending PR can be found here: ${{ fromJson(github) }}"
